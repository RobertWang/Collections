> 本文由 [简悦 SimpRead](http://ksria.com/simpread/) 转码， 原文地址 [www.cnblogs.com](https://www.cnblogs.com/2018-05-9-ygk/p/9259806.html) 注意在任何发送请求头之前不能有输出，不然会获取不到 header 头 setcookie(key,value, expires, 'path', domain); 在发送 cookie 时，会自动把 cookie 的值会被 urlencode 编码， PHP 收到 cookie 时，会自动解码，并复制到可变的 cookie 名称上。 如果不想 value 不被自动编码，可以使用 setrawcookie() 函数 setrawcookie() 函数发送未经过 URL 编码的 cookie 该函数和 setcookie 函数的用法差不多 PHP 的 urlencode 和 urldecode js 的 encodeURI 和 decodeURI